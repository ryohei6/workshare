<%= form_with(model: workshare, local: true) do |form| %>
  <% if workshare.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workshare.errors.count, "error") %> prohibited this workshare from being saved:</h2>

      <ul>
      <% workshare.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
